import React from 'react';
import { BaseProps } from './base';
declare const OrderByList: {
    (props: Partial<BaseProps>): React.JSX.Element;
    defaultProps: {
        height: string;
        fill: string;
        viewBox: string;
        predefinedClassName: string;
    };
};
export default OrderByList;
