import React from 'react';
import { BaseProps } from './base';
declare const FreeWindow: {
    (props: Partial<BaseProps>): React.JSX.Element;
    defaultProps: {
        height: string;
        viewBox: string;
        predefinedClassName: string;
    };
};
export default FreeWindow;
