import log from '../utils/log';
const version = typeof __VERSION__ !== 'undefined' ? __VERSION__ : globalThis.HUBBLE_VERSION || 'untranspiled source';
const existingVersion = globalThis.hubble && globalThis.hubble.VERSION;

if (existingVersion && existingVersion !== version) {
  throw new Error("hubble.gl - multiple versions detected: ".concat(existingVersion, " vs ").concat(version));
}

if (!existingVersion) {
  log.log(1, "hubble.gl ".concat(version))();
  globalThis.hubble = { ...globalThis.hubble,
    VERSION: version,
    version,
    log
  };
}

export default globalThis.hubble;
//# sourceMappingURL=init.js.map