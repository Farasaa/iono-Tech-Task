import React from 'react';
interface TimeTitleProps {
    value: number[];
    isEnlarged?: boolean;
    timezone?: string | null;
    timeFormat: string;
}
declare function TimeRangeSliderTimeTitleFactory(): ({ value, isEnlarged, timezone, timeFormat }: TimeTitleProps) => React.JSX.Element;
export default TimeRangeSliderTimeTitleFactory;
