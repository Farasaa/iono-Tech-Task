import Keyframes from './keyframes';

function getFeatures(layer) {
  return Object.keys(layer.config.visConfig);
}

class KeplerLayerKeyframes extends Keyframes {
  constructor(_ref) {
    let {
      layer,
      timings,
      keyframes,
      easings
    } = _ref;
    super({
      timings,
      keyframes,
      easings,
      features: getFeatures(layer)
    });
    this.layer = layer;
  }

  set(_ref2) {
    let {
      layer = undefined,
      timings,
      keyframes,
      easings,
      interpolators
    } = _ref2;
    if (layer) this.layer = layer;
    super.set({
      timings,
      keyframes,
      easings,
      interpolators
    });
  }

}

export default KeplerLayerKeyframes;
//# sourceMappingURL=kepler-layer-keyframes.js.map