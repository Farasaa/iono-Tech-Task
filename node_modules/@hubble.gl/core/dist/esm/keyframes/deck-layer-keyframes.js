import _defineProperty from "@babel/runtime/helpers/esm/defineProperty";
import Keyframes from './keyframes';

function getFeatures(keyframes) {
  return keyframes && keyframes[0] ? Object.keys(keyframes[0]) : [];
}

export default class DeckLayerKeyframes extends Keyframes {
  constructor(_ref) {
    let {
      id,
      features,
      timings,
      keyframes,
      easings
    } = _ref;
    super({
      timings,
      keyframes,
      easings,
      features: features || getFeatures(keyframes)
    });

    _defineProperty(this, "id", void 0);

    this.id = id;
  }

}
//# sourceMappingURL=deck-layer-keyframes.js.map